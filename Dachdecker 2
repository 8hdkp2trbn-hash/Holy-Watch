<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Dachdecker Ramdedovic — Godshorn / Langenhagen</title>
<meta name="description" content="Dachdecker Ramdedovic in Godshorn: Dacheindeckung, Sanierung, Reparatur und Notdienst rund um Langenhagen und Hannover.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@500;600;700;900&family=Inter:wght@400;500;600;700&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
  :root{
    --bg: #1B1E22;
    --bg-alt: #22262B;
    --bg-raised: #2B3036;
    --tile: #B8502E;
    --tile-dark: #8C3B22;
    --tile-light: #D06B45;
    --brass: #A9835A;
    --paper: #EDE7DC;
    --paper-dim: #AEA99D;
    --paper-faint: #78766F;
    --line: rgba(237,231,220,0.12);
    --line-strong: rgba(237,231,220,0.22);
    --font-display: 'Barlow Condensed', sans-serif;
    --font-body: 'Inter', sans-serif;
    --font-mono: 'IBM Plex Mono', monospace;
    --radius: 3px;
    --wrap: 1180px;
  }

  *{ box-sizing:border-box; }
  html{ scroll-behavior:smooth; }
  @media (prefers-reduced-motion: reduce){ html{ scroll-behavior:auto; } *{ animation-duration:0.001ms !important; animation-iteration-count:1 !important; transition-duration:0.001ms !important; } }

  body{
    margin:0;
    background:var(--bg);
    color:var(--paper);
    font-family:var(--font-body);
    font-size:16px;
    line-height:1.6;
    -webkit-font-smoothing:antialiased;
  }

  img,svg{ display:block; max-width:100%; }
  a{ color:inherit; text-decoration:none; }
  ul{ margin:0; padding:0; list-style:none; }
  h1,h2,h3{ margin:0; font-family:var(--font-display); font-weight:900; letter-spacing:0.01em; }
  p{ margin:0; }

  :focus-visible{ outline:2px solid var(--tile-light); outline-offset:3px; }

  .wrap{ width:100%; max-width:var(--wrap); margin:0 auto; padding:0 28px; }

  .eyebrow{
    font-family:var(--font-mono);
    font-size:12.5px;
    letter-spacing:0.14em;
    text-transform:uppercase;
    color:var(--tile-light);
    display:flex;
    align-items:center;
    gap:10px;
  }
  .eyebrow::before{ content:""; width:22px; height:1px; background:var(--tile-light); display:inline-block; }

  /* ---------- Buttons ---------- */
  .btn{
    display:inline-flex;
    align-items:center;
    gap:10px;
    font-family:var(--font-body);
    font-weight:600;
    font-size:15px;
    padding:13px 22px;
    border-radius:var(--radius);
    border:1px solid transparent;
    cursor:pointer;
    transition:transform .15s ease, background .15s ease, border-color .15s ease;
    white-space:nowrap;
  }
  .btn:hover{ transform:translateY(-1px); }
  .btn-primary{ background:var(--tile); color:#fff; }
  .btn-primary:hover{ background:var(--tile-light); }
  .btn-ghost{ background:transparent; color:var(--paper); border-color:var(--line-strong); }
  .btn-ghost:hover{ border-color:var(--brass); color:var(--brass); }
  .btn svg{ width:16px; height:16px; flex:none; }

  /* ---------- Header ---------- */
  .site-header{
    position:sticky; top:0; z-index:50;
    background:rgba(27,30,34,0.85);
    backdrop-filter:blur(10px);
    border-bottom:1px solid transparent;
    transition:border-color .25s ease, background .25s ease;
  }
  .site-header.is-scrolled{ border-bottom-color:var(--line); background:rgba(27,30,34,0.97); }
  .header-inner{ display:flex; align-items:center; justify-content:space-between; height:76px; }

  .logo{ display:flex; align-items:center; gap:12px; }
  .logo-mark{ width:34px; height:34px; flex:none; }
  .logo-text{ font-family:var(--font-display); font-weight:800; font-size:22px; line-height:1; letter-spacing:0.01em; }
  .logo-text small{ display:block; font-family:var(--font-mono); font-weight:400; font-size:10.5px; letter-spacing:0.12em; color:var(--paper-dim); margin-top:3px; text-transform:uppercase; }

  .main-nav{ display:flex; align-items:center; gap:34px; }
  .main-nav a{ font-size:14.5px; font-weight:500; color:var(--paper-dim); transition:color .15s ease; position:relative; }
  .main-nav a:hover{ color:var(--paper); }

  .header-actions{ display:flex; align-items:center; gap:14px; }
  .nav-toggle{ display:none; background:none; border:1px solid var(--line-strong); border-radius:var(--radius); width:44px; height:44px; align-items:center; justify-content:center; }
  .nav-toggle svg{ width:20px; height:20px; }

  @media (max-width: 860px){
    .main-nav{
      position:absolute; top:76px; left:0; right:0;
      background:var(--bg-alt);
      border-bottom:1px solid var(--line);
      flex-direction:column; align-items:flex-start; gap:0;
      max-height:0; overflow:hidden;
      transition:max-height .25s ease;
    }
    .main-nav.is-open{ max-height:320px; }
    .main-nav a{ width:100%; padding:16px 28px; border-bottom:1px solid var(--line); }
    .nav-toggle{ display:flex; }
    .header-actions .btn-primary span.long{ display:none; }
  }

  /* ---------- Tile divider ---------- */
  .tile-divider{ width:100%; height:26px; display:block; }
  .tile-divider .a{ fill:var(--tile-dark); }
  .tile-divider .b{ fill:var(--tile); }
  .tile-divider.flip{ transform:rotate(180deg); }

  /* ---------- Hero ---------- */
  .hero{
    position:relative;
    padding:96px 0 0;
    overflow:hidden;
    background:
      radial-gradient(ellipse 900px 500px at 82% -10%, rgba(184,80,46,0.22), transparent 60%),
      var(--bg);
  }
  .hero-inner{ display:grid; grid-template-columns:1.1fr 0.9fr; gap:56px; align-items:center; }
  .hero-eyebrow{ margin-bottom:22px; }
  .hero h1{
    font-size:clamp(42px, 6vw, 74px);
    line-height:0.98;
    color:var(--paper);
  }
  .hero h1 em{ font-style:normal; color:var(--tile-light); }
  .hero-sub{
    margin-top:22px;
    max-width:46ch;
    font-size:17.5px;
    color:var(--paper-dim);
  }
  .hero-actions{ display:flex; flex-wrap:wrap; gap:14px; margin-top:34px; }

  .hero-stats{ display:flex; gap:28px; margin-top:44px; }
  .hero-stat{ font-family:var(--font-mono); }
  .hero-stat .num{ font-family:var(--font-display); font-size:30px; font-weight:800; color:var(--paper); line-height:1; }
  .hero-stat .lbl{ font-size:11px; letter-spacing:0.08em; text-transform:uppercase; color:var(--paper-faint); margin-top:6px; }

  .hero-art{ position:relative; }
  .roof-illustration{ width:100%; height:auto; }

  .hero-bottom-band{ margin-top:80px; }

  @media (max-width: 940px){
    .hero-inner{ grid-template-columns:1fr; }
    .hero-art{ order:-1; max-width:340px; margin:0 auto 12px; }
    .hero{ padding-top:32px; text-align:left; }
  }

  /* ---------- Section shell ---------- */
  .section{ padding:100px 0; }
  .section-tight{ padding:70px 0; }
  .section-head{ max-width:640px; margin-bottom:56px; }
  .section-head h2{ font-size:clamp(32px, 4vw, 46px); margin-top:16px; color:var(--paper); }
  .section-head p{ margin-top:16px; color:var(--paper-dim); font-size:16px; }
  .bg-alt{ background:var(--bg-alt); }

  .reveal{ opacity:0; transform:translateY(18px); transition:opacity .6s ease, transform .6s ease; }
  .reveal.is-visible{ opacity:1; transform:none; }

  /* ---------- Services ---------- */
  .services-grid{ display:grid; grid-template-columns:repeat(3, 1fr); gap:1px; background:var(--line); border:1px solid var(--line); border-radius:var(--radius); overflow:hidden; }
  .service-card{ background:var(--bg); padding:34px 30px; transition:background .2s ease; }
  .service-card:hover{ background:var(--bg-raised); }
  .service-icon{ width:38px; height:38px; color:var(--tile-light); margin-bottom:22px; }
  .service-card h3{ font-family:var(--font-body); font-weight:700; font-size:17.5px; color:var(--paper); letter-spacing:0; }
  .service-card p{ margin-top:10px; font-size:14.5px; color:var(--paper-dim); line-height:1.65; }
  .services-note{ margin-top:26px; font-size:13px; color:var(--paper-faint); font-family:var(--font-mono); }

  @media (max-width: 860px){ .services-grid{ grid-template-columns:1fr; } }
  @media (min-width: 861px) and (max-width: 1080px){ .services-grid{ grid-template-columns:repeat(2, 1fr); } }

  /* ---------- Trust strip ---------- */
  .trust-grid{ display:grid; grid-template-columns:repeat(4, 1fr); gap:36px; }
  .trust-item{ border-top:2px solid var(--tile); padding-top:18px; }
  .trust-item h3{ font-family:var(--font-body); font-size:15.5px; font-weight:700; color:var(--paper); }
  .trust-item p{ margin-top:8px; font-size:14px; color:var(--paper-dim); }
  @media (max-width: 860px){ .trust-grid{ grid-template-columns:repeat(2, 1fr); } }
  @media (max-width: 520px){ .trust-grid{ grid-template-columns:1fr; } }

  /* ---------- Hours / plaque ---------- */
  .hours-layout{ display:grid; grid-template-columns:1fr 1fr; gap:60px; align-items:start; }
  .hours-table{ width:100%; border-collapse:collapse; font-family:var(--font-mono); font-size:15px; }
  .hours-table tr{ border-bottom:1px solid var(--line); }
  .hours-table tr:last-child{ border-bottom:none; }
  .hours-table td{ padding:16px 4px; }
  .hours-table td:first-child{ color:var(--paper-dim); text-transform:uppercase; letter-spacing:0.06em; font-size:12.5px; }
  .hours-table td:last-child{ text-align:right; color:var(--paper); }
  .hours-table tr.today td{ color:var(--tile-light); }
  .hours-table tr.today td:first-child::before{ content:"— "; }

  .plaque{
    position:relative;
    background:
      repeating-linear-gradient(135deg, rgba(255,255,255,0.025) 0 2px, transparent 2px 10px),
      linear-gradient(180deg, var(--bg-raised), var(--bg-alt));
    border:1px solid var(--line-strong);
    border-radius:var(--radius);
    padding:32px 30px;
  }
  .plaque::before, .plaque::after,
  .plaque .rivet-tl, .plaque .rivet-tr, .plaque .rivet-bl, .plaque .rivet-br{
    content:""; position:absolute; width:7px; height:7px; border-radius:50%;
    background:radial-gradient(circle at 35% 35%, #4a4f55, #16181b 70%);
    box-shadow:0 1px 0 rgba(255,255,255,0.06);
  }
  .plaque .rivet-tl{ top:12px; left:12px; }
  .plaque .rivet-tr{ top:12px; right:12px; }
  .plaque .rivet-bl{ bottom:12px; left:12px; }
  .plaque .rivet-br{ bottom:12px; right:12px; }

  .plaque-top{ display:flex; align-items:center; justify-content:space-between; }
  .plaque-label{ font-family:var(--font-mono); font-size:11.5px; letter-spacing:0.12em; text-transform:uppercase; color:var(--paper-faint); }
  .status-pill{ display:inline-flex; align-items:center; gap:8px; font-family:var(--font-mono); font-size:12.5px; font-weight:500; letter-spacing:0.04em; padding:6px 12px; border-radius:20px; border:1px solid var(--line-strong); }
  .status-dot{ width:8px; height:8px; border-radius:50%; background:var(--paper-faint); flex:none; }
  .status-pill.open{ color:#8FC49A; border-color:rgba(143,196,154,0.35); background:rgba(143,196,154,0.08); }
  .status-pill.open .status-dot{ background:#8FC49A; box-shadow:0 0 0 3px rgba(143,196,154,0.18); }
  .status-pill.closed{ color:var(--tile-light); border-color:rgba(184,80,46,0.35); background:rgba(184,80,46,0.08); }
  .status-pill.closed .status-dot{ background:var(--tile-light); }

  .plaque-status-line{ font-family:var(--font-display); font-size:30px; font-weight:800; margin-top:22px; color:var(--paper); }
  .plaque-detail{ margin-top:10px; font-size:14.5px; color:var(--paper-dim); font-family:var(--font-mono); }
  .plaque hr{ border:none; border-top:1px solid var(--line); margin:24px 0; }
  .plaque-call{ display:flex; align-items:center; justify-content:space-between; gap:16px; }
  .plaque-call .num{ font-family:var(--font-mono); font-size:17px; color:var(--paper); }

  @media (max-width: 860px){ .hours-layout{ grid-template-columns:1fr; } }

  /* ---------- Reviews ---------- */
  .review-wrap{ display:flex; align-items:center; gap:44px; flex-wrap:wrap; }
  .review-badge{ display:flex; align-items:baseline; gap:16px; }
  .review-badge .pct{ font-family:var(--font-display); font-size:88px; font-weight:900; color:var(--paper); line-height:0.9; }
  .review-meta{ font-family:var(--font-mono); font-size:13.5px; color:var(--paper-dim); line-height:1.7; }
  .stars{ display:flex; gap:4px; margin-bottom:6px; }
  .stars svg{ width:16px; height:16px; color:var(--tile-light); }
  .review-cta{ margin-left:auto; }
  @media (max-width: 700px){ .review-cta{ margin-left:0; } .review-badge .pct{ font-size:64px; } }

  /* ---------- Contact ---------- */
  .contact-layout{ display:grid; grid-template-columns:0.95fr 1.05fr; gap:0; border:1px solid var(--line); border-radius:var(--radius); overflow:hidden; }
  .contact-info{ background:var(--bg-alt); padding:44px 40px; }
  .contact-row{ display:flex; gap:16px; padding:20px 0; border-bottom:1px solid var(--line); }
  .contact-row:first-child{ padding-top:0; }
  .contact-row:last-child{ border-bottom:none; padding-bottom:0; }
  .contact-row svg{ width:20px; height:20px; color:var(--tile-light); flex:none; margin-top:2px; }
  .contact-row .lbl{ font-family:var(--font-mono); font-size:11px; letter-spacing:0.1em; text-transform:uppercase; color:var(--paper-faint); }
  .contact-row .val{ margin-top:4px; font-size:15.5px; color:var(--paper); line-height:1.55; }
  .contact-row .val a:hover{ color:var(--tile-light); }
  .contact-actions{ display:flex; gap:12px; margin-top:30px; flex-wrap:wrap; }
  .map-frame{ min-height:380px; background:var(--bg-raised); filter:grayscale(0.35) contrast(1.05); }
  .map-frame iframe{ width:100%; height:100%; border:0; display:block; min-height:380px; }

  @media (max-width: 860px){ .contact-layout{ grid-template-columns:1fr; } .map-frame{ min-height:300px; } .map-frame iframe{ min-height:300px; } }

  /* ---------- Footer ---------- */
  footer{ border-top:1px solid var(--line); padding:52px 0 34px; }
  .footer-grid{ display:flex; justify-content:space-between; gap:40px; flex-wrap:wrap; }
  .footer-brand .logo-text{ font-size:19px; }
  .footer-col h4{ font-family:var(--font-mono); font-size:11px; letter-spacing:0.1em; text-transform:uppercase; color:var(--paper-faint); margin-bottom:14px; }
  .footer-col p, .footer-col a{ font-size:14px; color:var(--paper-dim); display:block; margin-bottom:8px; }
  .footer-col a:hover{ color:var(--paper); }
  .footer-bottom{ margin-top:46px; padding-top:24px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:20px; flex-wrap:wrap; font-size:12.5px; color:var(--paper-faint); font-family:var(--font-mono); }
</style>
</head>
<body>

<header class="site-header" id="siteHeader">
  <div class="wrap header-inner">
    <a href="#top" class="logo">
      <svg class="logo-mark" viewBox="0 0 34 34" fill="none" aria-hidden="true">
        <path d="M2 20 L17 6 L32 20" stroke="#B8502E" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
        <path d="M6 18 V30 H28 V18" stroke="#EDE7DC" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <span class="logo-text">Ramdedovic<small>Dachdeckerbetrieb · Godshorn</small></span>
    </a>

    <nav class="main-nav" id="mainNav">
      <a href="#leistungen">Leistungen</a>
      <a href="#warum">Warum wir</a>
      <a href="#oeffnungszeiten">Öffnungszeiten</a>
      <a href="#bewertungen">Bewertungen</a>
      <a href="#kontakt">Kontakt</a>
    </nav>

    <div class="header-actions">
      <a class="btn btn-primary" href="tel:+491732170905">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
        <span class="long">+49 173 2170905</span>
      </a>
      <button class="nav-toggle" id="navToggle" aria-label="Menü öffnen" aria-expanded="false">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M3 6h18M3 12h18M3 18h18"/></svg>
      </button>
    </div>
  </div>
</header>

<main id="top">

  <!-- HERO -->
  <section class="hero">
    <div class="wrap hero-inner">
      <div class="reveal is-visible">
        <div class="eyebrow hero-eyebrow">Dachdeckerbetrieb · Godshorn / Langenhagen</div>
        <h1>Schicht für<br>Schicht <em>zuverlässig.</em></h1>
        <p class="hero-sub">Dachdecker Ramdedovic übernimmt Eindeckung, Sanierung, Reparatur und Notdienst rund um Godshorn, Langenhagen und Hannover — sauber gearbeitet, termingerecht geliefert.</p>
        <div class="hero-actions">
          <a class="btn btn-primary" href="tel:+491732170905">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
            Jetzt anrufen
          </a>
          <a class="btn btn-ghost" href="#kontakt">
            Anfahrt &amp; Kontakt
          </a>
        </div>
        <div class="hero-stats">
          <div class="hero-stat">
            <div class="num">08–16:30</div>
            <div class="lbl">Mo – Fr geöffnet</div>
          </div>
          <div class="hero-stat">
            <div class="num">100%</div>
            <div class="lbl">Weiterempfehlung</div>
          </div>
          <div class="hero-stat">
            <div class="num">3,1 km</div>
            <div class="lbl">Ø Anfahrt Umgebung</div>
          </div>
        </div>
      </div>

      <div class="hero-art reveal is-visible">
        <svg class="roof-illustration" viewBox="0 0 420 380" fill="none" aria-hidden="true">
          <defs>
            <linearGradient id="skyG" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0" stop-color="#2B3036"/>
              <stop offset="1" stop-color="#1B1E22"/>
            </linearGradient>
            <linearGradient id="roofG" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0" stop-color="#D06B45"/>
              <stop offset="1" stop-color="#8C3B22"/>
            </linearGradient>
          </defs>
          <rect x="0" y="0" width="420" height="380" fill="url(#skyG)" rx="6"/>
          <g transform="translate(45,60)">
            <path d="M0 130 L165 10 L330 130 Z" fill="url(#roofG)"/>
            <path d="M0 130 L165 10 L330 130 L330 150 L165 30 L0 150 Z" fill="#EDE7DC" opacity="0.9"/>
            <g stroke="#1B1E22" stroke-opacity="0.25" stroke-width="2">
              <path d="M33 106 L165 10"/>
              <path d="M66 122 L165 52"/>
              <path d="M99 138... " opacity="0"/>
            </g>
            <g fill="#1B1E22" fill-opacity="0.18">
              <circle cx="90" cy="98" r="9"/><circle cx="112" cy="98" r="9"/><circle cx="134" cy="98" r="9"/><circle cx="156" cy="98" r="9"/><circle cx="178" cy="98" r="9"/><circle cx="200" cy="98" r="9"/><circle cx="222" cy="98" r="9"/><circle cx="244" cy="98" r="9"/>
              <circle cx="101" cy="112" r="9"/><circle cx="123" cy="112" r="9"/><circle cx="145" cy="112" r="9"/><circle cx="167" cy="112" r="9"/><circle cx="189" cy="112" r="9"/><circle cx="211" cy="112" r="9"/><circle cx="233" cy="112" r="9"/>
            </g>
            <rect x="0" y="150" width="330" height="150" fill="#22262B" stroke="#3a4046" stroke-width="1"/>
            <rect x="145" y="200" width="40" height="100" fill="#16181b"/>
            <rect x="30" y="185" width="46" height="46" fill="#2B3036" stroke="#3a4046"/>
            <rect x="254" y="185" width="46" height="46" fill="#2B3036" stroke="#3a4046"/>
            <line x1="53" y1="185" x2="53" y2="231" stroke="#3a4046"/>
            <line x1="30" y1="208" x2="76" y2="208" stroke="#3a4046"/>
            <line x1="277" y1="185" x2="277" y2="231" stroke="#3a4046"/>
            <line x1="254" y1="208" x2="300" y2="208" stroke="#3a4046"/>
            <rect x="250" y="-38" width="16" height="55" fill="#3a4046"/>
          </g>
        </svg>
      </div>
    </div>

    <div class="hero-bottom-band">
      <svg class="tile-divider" viewBox="0 0 400 26" preserveAspectRatio="none" aria-hidden="true">
        <defs>
          <pattern id="tilesHero" width="40" height="26" patternUnits="userSpaceOnUse">
            <circle class="a" cx="10" cy="0" r="12"/>
            <circle class="a" cx="30" cy="0" r="12"/>
            <circle class="a" cx="50" cy="0" r="12"/>
            <circle class="b" cx="0" cy="13" r="12"/>
            <circle class="b" cx="20" cy="13" r="12"/>
            <circle class="b" cx="40" cy="13" r="12"/>
          </pattern>
        </defs>
        <rect width="400" height="26" fill="url(#tilesHero)"/>
      </svg>
    </div>
  </section>

  <!-- SERVICES -->
  <section class="section" id="leistungen">
    <div class="wrap">
      <div class="section-head reveal">
        <div class="eyebrow">Leistungen</div>
        <h2>Vom Erstgespräch bis zur letzten Ziegelreihe.</h2>
        <p>Ein Überblick über die klassischen Gewerke eines Dachdeckerbetriebs — Details und Ausstattung besprechen wir gern bei Ihnen vor Ort.</p>
      </div>

      <div class="services-grid reveal">
        <div class="service-card">
          <svg class="service-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M3 11 12 3l9 8"/><path d="M5 10v10h14V10"/><path d="M9 20v-6h6v6"/></svg>
          <h3>Dacheindeckung</h3>
          <p>Neueindeckung mit Ton-, Beton- oder Schieferdeckung — fachgerecht geplant und ausgeführt.</p>
        </div>
        <div class="service-card">
          <svg class="service-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M3 12a9 9 0 1 0 9-9"/><path d="M3 4v8h8"/></svg>
          <h3>Dachsanierung</h3>
          <p>Instandsetzung, Dämmung und energetische Aufwertung bestehender Dachflächen.</p>
        </div>
        <div class="service-card">
          <svg class="service-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M13 2 3 14h7l-1 8 11-14h-7l1-6z"/></svg>
          <h3>Reparatur &amp; Notdienst</h3>
          <p>Schnelle Hilfe bei Sturmschäden, undichten Stellen und losen Ziegeln.</p>
        </div>
        <div class="service-card">
          <svg class="service-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="4" width="16" height="16" rx="1"/><path d="M4 12h16M12 4v16"/></svg>
          <h3>Dachfenster &amp; Lichtkuppeln</h3>
          <p>Einbau und Austausch für mehr Tageslicht und Wohnkomfort unterm Dach.</p>
        </div>
        <div class="service-card">
          <svg class="service-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18M6 6v13a1 1 0 0 0 1 1h10a1 1 0 0 0 1-1V6"/><path d="M9 6V4h6v2"/></svg>
          <h3>Klempnerarbeiten</h3>
          <p>Dachrinnen, Fallrohre und Verblechungen in Zink oder Kupfer.</p>
        </div>
        <div class="service-card">
          <svg class="service-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M13 2 3 14h7l-1 8 11-14h-7z"/><circle cx="19" cy="5" r="2"/></svg>
          <h3>Blitzschutz</h3>
          <p>Fachgerechte Montage und Prüfung von Blitzschutzanlagen.</p>
        </div>
      </div>
      <p class="services-note">Beispielhafte Leistungsübersicht — genaues Angebot nach Absprache und Vor-Ort-Termin.</p>
    </div>
  </section>

  <!-- WARUM WIR -->
  <section class="section section-tight bg-alt" id="warum">
    <div class="wrap">
      <div class="section-head reveal">
        <div class="eyebrow">Warum wir</div>
        <h2>Handwerk mit kurzen Wegen.</h2>
      </div>
      <div class="trust-grid reveal">
        <div class="trust-item">
          <h3>Lokal verwurzelt</h3>
          <p>Ansässig in Godshorn — kurze Anfahrtswege in Langenhagen und Umgebung.</p>
        </div>
        <div class="trust-item">
          <h3>Persönlicher Kontakt</h3>
          <p>Direkte Absprache und ehrliche Einschätzung vor Ort statt Callcenter.</p>
        </div>
        <div class="trust-item">
          <h3>Saubere Ausführung</h3>
          <p>Sorgfältige Arbeit von der Baustelleneinrichtung bis zur Endreinigung.</p>
        </div>
        <div class="trust-item">
          <h3>Verlässliche Termine</h3>
          <p>Klare Terminplanung, damit Sie wissen, woran Sie sind.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- OEFFNUNGSZEITEN -->
  <section class="section" id="oeffnungszeiten">
    <div class="wrap">
      <div class="section-head reveal">
        <div class="eyebrow">Öffnungszeiten</div>
        <h2>Wann Sie uns erreichen.</h2>
      </div>

      <div class="hours-layout reveal">
        <table class="hours-table" id="hoursTable">
          <tbody>
            <tr data-day="1-5"><td>Montag – Freitag</td><td>08:00 – 16:30 Uhr</td></tr>
            <tr data-day="6"><td>Samstag</td><td>08:00 – 13:00 Uhr</td></tr>
            <tr data-day="0"><td>Sonntag</td><td>Geschlossen</td></tr>
          </tbody>
        </table>

        <div class="plaque">
          <span class="rivet-tl"></span><span class="rivet-tr"></span><span class="rivet-bl"></span><span class="rivet-br"></span>
          <div class="plaque-top">
            <span class="plaque-label">Status · Baustellenschild</span>
            <span class="status-pill" id="statusPill"><span class="status-dot"></span><span id="statusPillText">Wird geprüft …</span></span>
          </div>
          <div class="plaque-status-line" id="statusHeadline">Einen Moment …</div>
          <div class="plaque-detail" id="statusDetail">Öffnungszeiten werden geladen.</div>
          <hr>
          <div class="plaque-call">
            <span class="num">+49 173 2170905</span>
            <a class="btn btn-primary" href="tel:+491732170905">Anrufen</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- BEWERTUNGEN -->
  <section class="section section-tight bg-alt" id="bewertungen">
    <div class="wrap">
      <div class="eyebrow">Bewertungen</div>
      <div class="review-wrap reveal" style="margin-top:24px;">
        <div class="review-badge">
          <span class="pct">100%</span>
          <div class="review-meta">
            <div class="stars" aria-hidden="true">
              <svg viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.6 5.8 6.4.6-4.8 4.2 1.4 6.3L10 14.9 4.4 17.9l1.4-6.3L1 7.4l6.4-.6z"/></svg>
              <svg viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.6 5.8 6.4.6-4.8 4.2 1.4 6.3L10 14.9 4.4 17.9l1.4-6.3L1 7.4l6.4-.6z"/></svg>
              <svg viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.6 5.8 6.4.6-4.8 4.2 1.4 6.3L10 14.9 4.4 17.9l1.4-6.3L1 7.4l6.4-.6z"/></svg>
              <svg viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.6 5.8 6.4.6-4.8 4.2 1.4 6.3L10 14.9 4.4 17.9l1.4-6.3L1 7.4l6.4-.6z"/></svg>
              <svg viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.6 5.8 6.4.6-4.8 4.2 1.4 6.3L10 14.9 4.4 17.9l1.4-6.3L1 7.4l6.4-.6z"/></svg>
            </div>
            Weiterempfehlung · 1 Bewertung<br>Kundenservice &amp; angebotene Leistungen
          </div>
        </div>
        <div class="review-cta">
          <a class="btn btn-ghost" href="tel:+491732170905">Erfahrung machen &amp; anrufen</a>
        </div>
      </div>
    </div>
  </section>

  <!-- KONTAKT -->
  <section class="section" id="kontakt">
    <div class="wrap">
      <div class="section-head reveal">
        <div class="eyebrow">Kontakt</div>
        <h2>Sprechen wir über Ihr Dach.</h2>
      </div>

      <div class="contact-layout reveal">
        <div class="contact-info">
          <div class="contact-row">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
            <div>
              <div class="lbl">Telefon</div>
              <div class="val"><a href="tel:+491732170905">+49 173 2170905</a></div>
            </div>
          </div>
          <div class="contact-row">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
            <div>
              <div class="lbl">Adresse</div>
              <div class="val">Ziegelei Straße 31, Haus 33<br>30855 Langenhagen (Godshorn)<br>Deutschland</div>
            </div>
          </div>
          <div class="contact-row">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
            <div>
              <div class="lbl">Erreichbarkeit</div>
              <div class="val">Mo – Fr 08:00 – 16:30 Uhr<br>Sa 08:00 – 13:00 Uhr</div>
            </div>
          </div>
          <div class="contact-actions">
            <a class="btn btn-primary" href="tel:+491732170905">Jetzt anrufen</a>
            <a class="btn btn-ghost" target="_blank" rel="noopener" href="https://www.google.com/maps/dir/?api=1&destination=Ziegelei%20Stra%C3%9Fe%2031%2C%2030855%20Langenhagen">Route planen</a>
          </div>
        </div>
        <div class="map-frame">
          <iframe
            src="https://www.google.com/maps?q=Ziegelei+Stra%C3%9Fe+31,+30855+Langenhagen&output=embed"
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"
            title="Standort Dachdecker Ramdedovic">
          </iframe>
        </div>
      </div>
    </div>
  </section>

</main>

<footer>
  <div class="wrap">
    <div class="footer-grid">
      <div class="footer-brand">
        <span class="logo-text">Ramdedovic</span>
        <p style="margin-top:14px; max-width:32ch; color:var(--paper-dim); font-size:14px;">Dachdeckerbetrieb in Godshorn — Eindeckung, Sanierung, Reparatur und Notdienst.</p>
      </div>
      <div class="footer-col">
        <h4>Kontakt</h4>
        <a href="tel:+491732170905">+49 173 2170905</a>
        <p>Ziegelei Straße 31, Haus 33<br>30855 Langenhagen</p>
      </div>
      <div class="footer-col">
        <h4>Öffnungszeiten</h4>
        <p>Mo – Fr 08:00 – 16:30</p>
        <p>Sa 08:00 – 13:00 · So geschlossen</p>
      </div>
      <div class="footer-col">
        <h4>Seite</h4>
        <a href="#leistungen">Leistungen</a>
        <a href="#oeffnungszeiten">Öffnungszeiten</a>
        <a href="#kontakt">Kontakt</a>
      </div>
    </div>
    <div class="footer-bottom">
      <span>© <span id="year"></span> Dachdecker Ramdedovic</span>
      <span>Ziegelei Straße 31, Haus 33 · 30855 Langenhagen · Impressum bitte vervollständigen</span>
    </div>
  </div>
</footer>

<script>
  // Header scroll state
  const header = document.getElementById('siteHeader');
  window.addEventListener('scroll', () => {
    header.classList.toggle('is-scrolled', window.scrollY > 8);
  }, { passive:true });

  // Mobile nav
  const navToggle = document.getElementById('navToggle');
  const mainNav = document.getElementById('mainNav');
  navToggle.addEventListener('click', () => {
    const open = mainNav.classList.toggle('is-open');
    navToggle.setAttribute('aria-expanded', open ? 'true' : 'false');
  });
  mainNav.querySelectorAll('a').forEach(a => a.addEventListener('click', () => {
    mainNav.classList.remove('is-open');
    navToggle.setAttribute('aria-expanded', 'false');
  }));

  // Scroll reveal
  const revealEls = document.querySelectorAll('.reveal');
  const io = new IntersectionObserver((entries) => {
    entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('is-visible'); });
  }, { threshold:0.15 });
  revealEls.forEach(el => io.observe(el));

  // Footer year
  document.getElementById('year').textContent = new Date().getFullYear();

  // Live opening-status based on Europe/Berlin time
  (function(){
    const HOURS = {
      1: [8*60, 16*60+30], 2: [8*60, 16*60+30], 3: [8*60, 16*60+30],
      4: [8*60, 16*60+30], 5: [8*60, 16*60+30],
      6: [8*60, 13*60], 0: null
    };
    const DAY_NAMES = ['Sonntag','Montag','Dienstag','Mittwoch','Donnerstag','Freitag','Samstag'];

    function berlinNow(){
      const parts = new Intl.DateTimeFormat('de-DE', {
        timeZone:'Europe/Berlin', weekday:'short', hour:'2-digit', minute:'2-digit', hour12:false
      }).formatToParts(new Date());
      const map = {};
      parts.forEach(p => map[p.type] = p.value);
      const weekdayMap = { So:0, Mo:1, Di:2, Mi:3, Do:4, Fr:5, Sa:6 };
      return { day: weekdayMap[map.weekday], minutes: parseInt(map.hour,10)*60 + parseInt(map.minute,10) };
    }

    function fmt(mins){
      const h = Math.floor(mins/60).toString().padStart(2,'0');
      const m = (mins%60).toString().padStart(2,'0');
      return h+':'+m+' Uhr';
    }

    function nextOpening(fromDay){
      for (let i=1; i<=7; i++){
        const d = (fromDay + i) % 7;
        if (HOURS[d]) return { day:d, offset:i, open:HOURS[d][0] };
      }
      return null;
    }

    const pill = document.getElementById('statusPill');
    const pillText = document.getElementById('statusPillText');
    const headline = document.getElementById('statusHeadline');
    const detail = document.getElementById('statusDetail');
    const rows = document.querySelectorAll('#hoursTable tr');

    const { day, minutes } = berlinNow();
    const todayHours = HOURS[day];
    const isOpen = todayHours && minutes >= todayHours[0] && minutes < todayHours[1];

    if (isOpen){
      pill.classList.add('open');
      pillText.textContent = 'Geöffnet';
      headline.textContent = 'Gerade geöffnet';
      detail.textContent = 'Schließt heute um ' + fmt(todayHours[1]) + '.';
    } else {
      pill.classList.add('closed');
      pillText.textContent = 'Geschlossen';
      if (todayHours && minutes < todayHours[0]){
        headline.textContent = 'Öffnet heute';
        detail.textContent = 'Öffnet um ' + fmt(todayHours[0]) + '.';
      } else {
        const next = nextOpening(day);
        if (next){
          headline.textContent = next.offset === 1 ? 'Öffnet morgen' : 'Aktuell geschlossen';
          detail.textContent = (next.offset === 1 ? 'Morgen' : DAY_NAMES[next.day]) + ' ab ' + fmt(next.open) + ' erreichbar.';
        }
      }
    }

    rows.forEach(row => {
      const key = row.getAttribute('data-day');
      const match = key === String(day) || (key === '1-5' && day >= 1 && day <= 5);
      if (match) row.classList.add('today');
    });
  })();
</script>

</body>
</html>
